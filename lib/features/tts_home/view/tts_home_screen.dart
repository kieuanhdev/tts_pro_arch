import 'package:flutter/material.dart';

class TtsHomeScreen extends StatelessWidget {
  const TtsHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TTS Pro - Day 1')),
      body: const Center(child: Text('du an dang phat trien')),
    );
  }
}
